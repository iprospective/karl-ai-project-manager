#!/bin/bash
# wordpress.sh — synchro spécifique wordpress (À IMPLÉMENTER).
# On commence par PrestaShop/test ; le squelette est prêt pour la suite.

wordpress_sync_files() {
  warn "[wordpress] sync_files pas encore implémenté — voir lib/presta.sh comme modèle."
}

wordpress_adapt_db() {
  warn "[wordpress] adapt_db pas encore implémenté — voir lib/presta.sh comme modèle."
}
